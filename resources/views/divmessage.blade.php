
<chat-messages :messages="messages"></chat-messages>
     {{-- @foreach($chat as $c)

                                    <div class="header">
                                        <strong class="primary-font">
                                            {{$c->name  }}
                                        </strong>
                                    </div>
                                    <p>
                                        {{ $c->message }}
                                    </p>
     @endforeach --}}
